import { addNavButtonListener, addCartButtonListeners, animateGallery } from "./util.js";

addNavButtonListener();


animateGallery();

// TODO: Might need to add logic that checks if there are buttons first
addCartButtonListeners();
